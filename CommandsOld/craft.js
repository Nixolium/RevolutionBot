module.exports = function (message) {
    let words = message.content.split(/\s+/)
    id = message.author.id;
    let target = id;
    functions.sendMessage(message.channel, "Coming soon")
    //initiate battle
}